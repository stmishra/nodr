{>layout/}
{<content}
  <div class="container-fluid">
	<div class="row-fluid">
	{#result}
		<div class="span2">
		</div>
		<div class="span10">
			<strong>{title}</strong>
			<p class="text-left">{post}</p>
		</div>
	{/result}
	</div>
  </div>
{/content}